INSTALL PROCESS
===============

faire un git config --local user.email quotek-dev@quotek.io dans /quotek
Ajouter le cronjob scheduler.php toutes les minutes

BUGS
====

 * visualize: les champs from/to doivent être convertis en UTC
 * Fix backtest start: progress bar undefined !
 * Fix backtest graphs, do not use current time to display values but simulation time (like logs)
 * Bug de display des parties de l'app en plein chargement !

PRIO
====

 * add inmem_history setting in configuration.
 * create logs plugin
 * Implement genetics backtests result view
 * stderr viewer on dashboard/statusbar, in case of strat compile failure, etc..

OTHER
=====

* bulk
* snapshots actions
* paging

